{
  "name": "vite-test",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "vite apps/$npm_config_app_id",
    "build": "tsc -p ./apps/$npm_config_app_id && vite build apps/$npm_config_app_id",
    "preview": "vite preview apps/$npm_config_app_id"
  },
  "dependencies": {
    "preact": "latest",
    "twind": "^0.16.16",
    "@twind/preact": "^1.0.5"
  },
  "devDependencies": {
    "vite": "^2.8.0",
    "@preact/preset-vite": "^2.1.5",
    "@types/node": "^17.0.18",
    "typescript": "4.6.1-rc"
  }
}
