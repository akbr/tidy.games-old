{
  "name": "test",
  "private": true,
  "scripts": {
    "start": "wmr --public apps/$npm_config_app_id/",
    "build": "wmr build --public apps/$npm_config_app_id/ --visualize true",
    "serve": "wmr serve",
    "build-node": "tsc --project tsconfig-node.json && tsc-alias -p tsconfig-node.json",
    "heroku-postbuild": "npm run build-client --app_name=$APP_NAME && npm run build-servers"
  },
  "dependencies": {
    "@twind/preact": "^1.0.5",
    "preact": "^10.6.6",
    "twind": "^0.16.16"
  },
  "devDependencies": {
    "typescript": "4.6.1-rc",
    "tsc-alias": "^1.6.0",
    "wmr": "^3.7.2"
  }
}
